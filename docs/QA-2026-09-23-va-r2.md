# QA 2026-09-23: VA journey on staging, round 2

Walked on https://alphaos-staging.vercel.app (mock-armed) as
staging-va@alphaos.test, with staging-designer@alphaos.test for the designer
steps and a signed-out browser for the customer proof page. Headless, laptop
1280x800 and phone 390x844. Screenshots: `var/e2e-shots/va-r2/` (gitignored).
Test order: Etsy 4177310554 (Krista, customer email set to
r2-test@example.com), taken from awaiting_details to complete.

## Walked

1. Round 1 fixes: phone Home "Do first" rows are links; Orders search by
   buyer name finds the Etsy order; no hydration error on /orders; Complete
   details lists the 4 styles; after a photo the button reads "Save & send to
   production" and moves the order to ready_to_assign. The browser photo
   upload now works on staging (round 1 R2 CORS P1 is resolved).
2. Assign to Staging Designer; designer Start, upload STAGING-TEST-r2.png,
   Submit for QC (phone board). QC: compare viewer, checklist, shortcuts
   (1-5, A, F, Enter, ?), fail with feedback (order back to in_design,
   designer sees the reason), designer adds v2 and resubmits, pass with the
   typed signature, email preview, send (message `sent`, mock Gmail id),
   order awaiting_approval.
3. Messages: Needs you, Waiting to send (sent one draft), All mail, compose
   from an order with the preview step, Show/Hide suppressed, link an
   unmatched Etsy buyer message to the order.
4. Customer proof page on the phone: request a change with a checkbox, note
   and a pin (order back to design, designer sees the note and the pin),
   revised proof sent with the revision-ready email, customer approves.
5. Print: Sent to print (Gelato), Add tracking, order shipped, "Shipment
   created in Etsy" writeback shown; Delivered then Complete via the Orders
   bulk bar.
6. Customers list, search, customer page (laptop and phone); every phone tab
   (Home, Today, Orders, Messages, More drawer); Today counts match the
   database (13 details, 13 unassigned, 2 photo nudges; the third
   awaiting_photos order is under the 2 day nudge age). No page-level
   horizontal scroll on any phone page walked.

## Fixed (branch commits)

- P2 `e19ab0f` Top bar search did nothing: `/orders?q=...` hit the view
  redirect, which dropped `q`. The redirect now keeps every param and a search
  opens in All open.
- P2 `2235a6b` A style the VA picks on Complete details / New order was shown
  as "Defaulted to cartoon, not matched, please confirm" (not saved as a
  hand-set). It now sets `style_locked`.
- P2 `e8058f8` After one QC fail, the customer's FIRST proof used the
  "Your revised PixArt portrait is ready" template (revisionCount counts QC
  fails). Revision wording now needs a proof already sent to the customer or
  a customer-side move back to design.
- P2 `611519f` Orders list showed complete / delivered / cancelled orders as
  "Overdue" in rose. Same rule as the Overdue view now.
- P2 `1b31a4a` Customer page showed the Overdue badge on a complete order.
- P2 `95415df` QC shortcut legend and its "?" button sat on the phone tab bar,
  over the Home tab. Shown from lg up only.

## P3 (logged, not fixed)

- Complete details does not prefill "Product / category" from the Etsy
  listing title, and saving writes a null item title: the designer card says
  "Custom portrait", Orders says "No item details".
- Activity feed: the first assignment is logged as "Reassigned"; raw keys show
  as "Print.Manual Started", "Proof.Viewed", "Email.Sent", "Asset.Uploaded",
  "Message.Received".
- An older proof link, after a newer proof was sent and approved, still reads
  "Changes requested ... We'll send an updated proof soon" under "Your
  portrait is ready".
- Order page shows "Designer deadline 24 hours left" on shipped and complete
  orders; the QC header shows "in QC 19m" on a complete order.
- Finishing a shipped order takes two bulk status changes (Delivered, then
  Complete); the order page says "Waiting for delivery" with no button.
- A shipped order is labelled "Completed With Tracking" before it is complete.
- Stage drafts ("Now printing", "Shipped") stay in Waiting to send after the
  order is complete; the customer page lists drafts as "Outbound" with no
  draft marker.
- A reply linked by hand from Needs you is never classified, so no
  approval / revision suggestion follows even when the order awaits approval.
- QC email preview overlay has no dialog role or aria-modal and no Escape.
- Today: "Show 5 more of 5" (round 1, still there).
- Phone: `OPTIONS /` returns 400 on each page load (round 1, still there).

## Staging environment notes (not code)

- `businesses.gmail_history_id` came over from production (25738830), far
  above the mock mailbox ids (1000 + n), so a poll never saw mock mail. Set to
  2076 on staging for this walk (one mock Etsy buyer message arrived,
  unmatched). A re-clone brings the production value back.
- The mock "customer replies to the proof" only works when the poll lands on
  the same serverless instance that sent the proof (the list lives in memory),
  so on the Vercel preview it did not arrive. With no Alpha relay on staging
  the reply classifier cannot run either, so the VA-confirmed approval
  suggestion cannot appear on staging.
