# AlphaOS pre-rollout program, 2026-09-23

## 2026-09-24 11:00: final polish merged (ca98ee5, main matches); READY TO DEPLOY at ~13:05
- Leftovers fixed: shared plural() helper, Home/Today reply rows name the customer, nav "Boards" (/board) + "Designers" (/designers), style names as words, tour end cards, single Alpha AI entry (top bar). Lint + build green; ci-local + test:tour rerunning on the final commit.
- Deploy 5130939..ca98ee5 to production when the window opens (monitor armed): `vercel deploy --prod --yes --token "$VERCEL_TOKEN_VISION" --scope team_hdxl43AlgWsesgr06UyM3Lnk` from alphaos-wt-alpha, then verify health, both owner links, phone tour.

## 2026-09-24 10:10: all three gauntlet lanes MERGED (5130939, main matches), not yet deployed
- Admin lane: 46 fixes (floating Alpha button removed, phone header shows workspace name, Money renamed + aligned + Mark paid drawer, Health in plain words, Settings layout, import-rule boxes, login keeps the email, 44px phone buttons); round 4 = 10 on all admin screens. Designer lane: 10 everywhere but one unreproducible warning. VA lane: 10 everywhere but Home 9.
- Leftovers being fixed in one final pass: "1 email need a reply" plural, squashed boxes in new-order-form, Home "Reply" x3, sidebar "Designers" vs "Designer Roster", raw style keys in chips, tour done-card line.
- Deploy: production still 4b52bcf; the team's daily deploy allowance frees ~13:05 AEST (monitor armed); deploy 5130939+ then.

## 2026-09-24 08:30: VA gauntlet live
- NOT YET LIVE: production deploy refused (team's free daily deploy allowance used up by the day's staging/preview builds); production still runs 4b52bcf. Deploy 3366c16+ when the 24 h window frees (see time below) or when the last two lanes merge, whichever is later. VA lane gauntlet, 4 rounds, round 4 = 10 on every VA screen except Home 9 (23 fixes: Today, Orders chips/columns/search, QC hydration + send button + phone preview, assign, complete details, order page, print, messages, customers; "?" menu item renamed "Try it myself"). ci-local 22/22, lint, build. Left for other lanes: Home "Reply" repeated, sidebar "Designers" + "Designer Roster" naming, three Alpha AI buttons, raw style keys in chips.
- 09:25: designer gauntlet MERGED (a616c36; 4 rounds, all screens 10 but one unreproducible phone-board hydration warning; fixes to board card, My Board, My Week, Home, Quick guide, 44px header buttons, phone shell tidy); ci-local 22/22, lint, build. Admin lane still running. Deploy window frees ~13:05 AEST; deploy then (or after the admin merge, whichever is later).

## 2026-09-24 07:45: tour fix + simple telling live
- Production = 4b52bcf (main matches): "?" > Watch how it works fixed on phone and after completion (components/shell/lazy-extras.tsx replayed the start without its mode), ~300 ms Suspense delay removed, every step now two plain sentences (owner: "some telling, simple text, not an essay"). Verified live on phone and laptop.
- Owner tour state: my headless probes pressed Esc on the welcome, which counts as Later (3 = quiet for good); both owners' user.onboarding reset to null so they see the welcome fresh. Rule: never probe the tour on an owner's account; use a staging login.

## 2026-09-24 07:20: PRODUCTION IN SINGAPORE
- Cutover done by the region agent (docs/REGION-MOVE-2026-09-24.md): Neon project floral-truth-37733980 (resource alphaos-prod-sin, aws-ap-southeast-1, Postgres 18.6), Vercel functions sin1 (x-vercel-id syd1::sin1), production deployed from 5960e29 and merged as 1160a4b (main matches). All 37 tables, 76 policies, grants matched; sync healthy on the new database; both .env.local files and the secrets file updated (old URLs kept as *_OLD_USEAST for rollback; rollback = set the two env vars back, regions iad1, redeploy).
- Warm full-page TTFB from Melbourne now 0.35 to 0.39 s (was 0.41 to 0.45 s, and 0.55 to 0.60 s before the speed pass); Asia users gain far more.
- Vercel PREVIEW env DATABASE_URL/DIRECT_URL now point at the STAGING database (previews used to hit production).

## 2026-09-24 06:50: load incident
- Load average hit 190 (four CSS book bakes by the daemon + five AlphaOS agents building). Gauntlet agents paused 15 min then throttled (one dev server, nice -n 15, no parallel builds). While cleaning "orphan" dev servers I killed the CSS lane (:3007) and bake app (:3005) by the parent-pid heuristic; both restored by hand (nohup next dev -p 3007 in storybook-generator-github, -p 3005 in storybook-generator-wt-r2). Rule recorded in memory: never kill by parent pid.
- Neon Singapore resource created in this session with the owner's yes (`vercel integration add neon --plan free_v3 -m region=sin1`, SIN_ prefix, development env); the region agent is running the cutover.

## 2026-09-24 06:40: show-don't-tell tour live; final gauntlet running
- Production = 07467ed (main matches): the rebuilt tour (ghost pointer performs the real action, "Your turn" completes on the real action, "Watch how it works" 30 to 36 s per role, /help one screen with Show me per step; docs/TOUR.md, judge round 3 = 10 everywhere). Verified live as Ayser: welcome in ~210 ms, pointer moving in ~300 ms, watch mode drives /orders, laptop and phone.
- test:tour on a COLD dev server can time out on the 10 s waits after Done (compile latency on this iMac); the agent's warm run passed 706/706. If it fails once, rerun warm before treating it as a defect.
- Disk hit 373 MB free at 05:50: removed 21 merged agent worktrees + 6 old alphaos-wt-* worktrees, the scratch clone, package caches; now ~6.7 GB free. Merged worktrees are safe to remove (`git worktree remove --force`), the branches stay.
- Final real-user gauntlet running (3 agents: VA, designer phone-first, admin + shell), each looping debug, uncamel, aesthetics, test to a 10 per screen at both sizes; docs/QA-2026-09-24-gauntlet-<role>.md. Merge, verify, deploy after.
- 06:15: owner said YES to the free Neon database in Singapore via the Vercel integration ("yes do free neon in singapore sure"); region agent a78960fa6c76b0302 is running the cutover (docs/REGION-MOVE-2026-09-24.md when done). Owner also asked for simple telling on each tour step (two short sentences, not an essay); tutorial agent ab84ef3bbfd2712ea owns that plus the phone/post-completion "Watch" defect.

## 2026-09-24 05:30: speed work deployed; region move needs an owner yes
- Production = a29092c (main matches): speed pass live (in-app clicks skip the middleware hop, parallel reads, display-size photos, driver out of the client bundle, skeletons + prefetch + router cache, static-asset service worker). docs/PERF.md has before/after tables.
- Remaining latency is geography: functions in iad1 next to a Neon database in us-east-1, users in Melbourne and South East Asia (~280 ms per hop). Plan: Neon aws-ap-southeast-1 + Vercel sin1. BLOCKED on an owner decision: the free path (Vercel Neon integration, sin1) is stopped by the Vercel spend guard; the two Neon orgs NEON_API_KEY can see are a paid Launch org (support@aystudios.io) and the free org of the soft-blocked old almacorp team. Prep is done (pg_dump 17.11 in the region agent's scratchpad, counts, grants script): see the region agent's notes in the worktree agent-a78960fa6c76b0302 and docs/PERF.md. Production is Postgres 17.11.
- Tutorial rebuild (show, don't tell) still in flight: worktree agent-ab84ef3bbfd2712ea, docs/TOUR.md.

## 2026-09-24 morning: polish deployed, two lanes in flight
- Production = 72e3bb2 (main matches): 51 polish fixes from the two P3 passes (VA/admin/smoke + designer/security) and migration 0040 (RLS tighten round 2); ci-local 22/22, lint, build; both owners verified on every admin page.
- Owner ask 2026-09-23 night: (a) the tutorial must SHOW, not tell (ghost cursor performs the real action, then "your turn", watch-only auto-play, one-line copy, judge loop to 10 per role) and (b) the SOFTWARE must load instantly on any connection (throttled Slow/Fast 3G targets, app-shell service worker, smaller JS, streaming, prefetch). Both were cut off by a usage limit at ~03:30 and resumed at 04:10: tutorial = worktree agent-ab84ef3bbfd2712ea (docs/TOUR.md), speed = worktree agent-a5fe02b753cc93c67 (docs/PERF.md). When they land: merge, ci-local + lint + build + test:tour, staging, production, then the final real-user gauntlet (debug, uncamel, aesthetics, test; laptop and phone; loop until every role scores 10).

## RESUME NOTE (2026-09-23 late): superseded above
- Latest production deploy: task/alpha-program 6b0d660 (= main), includes per-person auto-login links (docs/LOGIN-LINKS.md, `npx tsx scripts/login-link.ts <email>`, migration 0039). Owner accounts + links + passwords live in ~/Documents/ai-employee-agent/.local/prod-secrets.env (ALPHAOS_AYSER_*, ALPHAOS_YOUSIF_*, ALPHAOS_LOGIN_LINK_*); both proven to sign in on production.
- Owner rule: any owner ask for the AlphaOS link is answered with their link + base link + email + password (knowledge/preferences/alphaos-link-always-with-passwords.md).
- Next step when work resumes: nothing pending from the program. Optional: the P3 lists in docs/QA-2026-09-23-*.md; Yousif's two asks (Shopify apps permission tick, email switches) remain his.
- No screen, desk or shop holds are owned by this session.

## DONE 2026-09-23 21:57 AEST: PRODUCTION DEPLOYED
- alphaos-kappa.vercel.app runs task/alpha-program bb5abde (main fast-forwarded to it). Vercel production DATABASE_URL now uses the app_user role: row-level security is ENFORCED in production for the first time. Migrations 0036 (onboarding), 0037 (session revocation), 0038 (RLS tighten) applied. Verified: admin sign-in, dashboard, orders (data visible), health all 200.
- Round 3 smoke on staging: all steps PASS, verdict ready. Its one data follow-up (due date slipping a day on save) was fixed before deploy (lib/time.ts dateInputValue/parseDueDate, test:due-date). Remaining smoke follow-ups (P3): designer phone 400 on a root request, shipped orders labelled "Completed With Tracking" + Overdue in the list until completed, wrong "sign in straight away" wording after resetting a deactivated user, Mark paid has no confirmation. Logged in docs/QA-2026-09-23-smoke-r3.md.
- Rollout is now a people step: create the real VA and designer accounts from Designers > Team and sign-ins; each gets the tour on first sign-in and the Quick guide at /help. Emails to buyers stay OFF until Yousif flips Settings > Customer Email (sending, and stage auto-send). Shopify apps permission still needs Yousif's tick.

## Incident 2026-09-23 21:14 to 22:10 AEST: staff-session Shopify sync failed ("Failed query: begin")
- Cause: the local sync (LaunchAgent com.almacorp.alphaos-shopify-sync, worktree .env.local) connects as app_user; that role's password stopped matching at 21:14 (someone or something reset it before the lead did at 21:56; not identified) and again after the lead's reset. Fix: both .env.local files (worktree + main checkout) now carry the app_user URL from .local/prod-secrets.env (backups .env.local.bak-2026-09-23). Rule: whenever app_user's password changes, update Vercel production DATABASE_URL AND both .env.local files together.

## History

## Branch state
- Integration branch: `task/alpha-program` (worktree ~/Documents/projects/alphaos-wt-alpha). Production runs this branch; main is stale.
- Merged today: designer-from-roster (de13ce2), test bench + staging (9a5eb91, 2d6f402), PixArt emails (0a0854f, 4c015a3), VA wizard (7aca6ca), test cleanup fixes.
- Verified on the merged branch: `scripts/ci-local.sh` 18/18 suites, lint, build, `npm run test:tour` 682 checks green.
- Staging: https://alphaos-staging.vercel.app runs the merged branch (redeploy: `scripts/staging/deploy-preview.sh`, see docs/STAGING.md). Logins in ~/Documents/ai-employee-agent/.local/alphaos-staging.env.

## Round 1 results (merged into task/alpha-program unless noted)
- Admin lane MERGED (aa1a65d): fixed Settings crash on a shop with no credentials. OPEN P1: no way to add a VA or deactivate a designer from the UI (build before rollout). P2s in docs/QA-2026-09-23-admin.md.
- VA lane MERGED (535c574): fixed phone Home taps, Orders search by customer name, hydration error 418 on /orders, Complete details style list. OPEN P1 (staging only): R2 bucket CORS lacks the staging origin so uploads fail there (scripts/set-r2-cors exists; adding the origin touches the prod bucket config, do it deliberately). P2: style name missing in "Defaulted to , not matched".
- Security lane MERGED (be205e4): fixed designers seeing the customer email thread + full name (P1), 500 on commenting an unseen order, middleware role coverage for every page, raw storage error on upload page, `..` in upload keys. OPEN P1: sessions never re-checked (deactivated user or role change keeps access up to 30 days; copied cookie survives sign-out). P2: no per-IP login limit. Verdict: switching prod to app_user is safe and recommended.
- Designer lane MERGED: fixed customer email leaking into the designer card download (P1), photos/versions not openable full size (P1). OPEN P2: board/Home/My Week show the customer due date instead of the designer's 24h deadline ("overdue 9d" on new work), designer search lands on /board, unstarted cards say uploads locked, Replace only adds a version. Staging note: QC Pass fails there because email is disarmed. Steps 5 and 6 not walked.
- NOT YET RE-VERIFIED after these merges: run scripts/ci-local.sh + lint + build + test:tour, then redeploy staging and run round 2 (the unwalked steps are listed in each QA doc).

## Round 2 in flight (evening)
- Merged branch re-verified after round 1: ci-local 18/18, lint, build green; staging redeployed; fix/photo-request-404 (r2-cors script) merged.
- R2 bucket CORS now lists production AND staging (it was missing production before; the script replaces the rule set, pass both origins every time).
- Prod DB roles checked: app_user exists with LOGIN and no BYPASSRLS; 27 of 35 tables have RLS; DATABASE_URL still neondb_owner. Switch = set a fresh app_user password (ALTER ROLE on DIRECT_URL), store it in ~/Documents/ai-employee-agent/.local/prod-secrets.env, set Vercel production DATABASE_URL to the pooled URL with user app_user, redeploy, verify.
- Agents running: (a) team management (add VA/admin, deactivate/reactivate, reset password) + per-request session re-check, closing the admin and security P1s; (b) designer/VA P2s (designer deadline shown, search, upload copy, Add new version, style name, Save details); (c) staging armed with mock Gmail/print/shop credentials + MOCK_INTEGRATIONS=1 so the full journey can be walked.
- After they land: merge, ci-local + lint + build + test:tour, redeploy staging, round 2 walk of the unwalked steps (VA 4-7, designer 5-6, admin 3-9, security 6-8), then production.

## Round 2 status (late evening)
- All three round-2 prep branches MERGED (team management + session revocation dda82fc, designer/VA P2s 3acf7b1, staging mocks 6408314); ci-local 19/19, lint, build, test:tour 682/682 (one earlier run had a single 30 s click timeout in "later + focus", passed on rerun; treat as a flake unless it repeats).
- Staging redeployed with mocks armed; the full mock journey (assign, upload, QC, proof approve, print, tracking, shipped) passed with no outside calls.
- Round 2 testers running (VA, designer, admin, security) on the unwalked steps plus re-verification of every round 1 fix; QA docs will be docs/QA-2026-09-23-<lane>-r2.md. Merge them, re-verify, then production.

## Round 2 results (all MERGED, ef84656; ci-local 20/20, lint, build, test:tour 682/682; staging redeployed)
- Designer lane: journey end to end OK, no P1; fixed paid-orders count, completed card countdown, My Week on-time rule, deactivate warning.
- Admin lane: no P1; fixed UTC times (now Melbourne with AEST/AEDT), template reset + save confirmation, stage auto-send switch, Business card (name/logo), shop styles from Portrait Styles only, void pending earning.
- Security lane: no P1; fixed reactivation reviving old cookies, per-IP login limit, double-click sending two proof emails, URL escaping in email HTML, figure count limit + raw SQL echo, mocks refused on production, Ask Alpha order scoping, migration 0038 (RLS tighten). Documented: 8 wider-than-needed RLS policies (design call), 12 P3s.
- VA lane: no P1; fixed top-bar search, style "defaulted" label, first proof email wording after a QC fail, false Overdue on finished orders (list + customer page), QC "?" button over the phone Home tab.
- Digital import: Shopify/Etsy digital items now import as digital (option/variant text, no-shipping, is_digital); conflicts go to review; re-resolve heals (never flips an order with a print job).
- Production app_user password set 2026-09-23 (ALTER ROLE), verified connecting; stored as ALPHAOS_APP_USER_PASSWORD + ALPHAOS_APP_USER_DATABASE_URL in ~/Documents/ai-employee-agent/.local/prod-secrets.env. Vercel production DATABASE_URL NOT yet switched.
- Round 3 smoke walk (all roles, final code) running; on PASS: switch Vercel DATABASE_URL, deploy prod, verify, fast-forward main.

## Pending: four E2E tester branches (worktree-agent-* under ~/Documents/projects/alphaos/.claude/worktrees/)
- VA lane: ae9c4fa4636aeb8c4, docs/QA-2026-09-23-va.md
- Designer lane: a666ff5d4f3d42360, docs/QA-2026-09-23-designer.md
- Admin lane: af7f00338d1a9daf7, docs/QA-2026-09-23-admin.md
- Security lane: a1597b0130e80bc3e, docs/QA-2026-09-23-security.md
Next: merge each into task/alpha-program, resolve conflicts (usually package.json), rerun ci-local + build, redeploy staging, run round 2, then production.

## Before production deploy
1. Switch production DATABASE_URL to the `app_user` role (RLS is defined but NOT enforced in prod today because the app connects as neondb_owner). Steps: header of lib/db/migrations/0001_rls_policies.sql; ALTER ROLE app_user WITH LOGIN PASSWORD on the owner connection, set Vercel production DATABASE_URL (pooled) to app_user, keep DIRECT_URL on the owner, redeploy, verify sign-in + orders as admin.
2. Migration 0036 (user.onboarding) runs at build via vercel-build. Every existing user sees the welcome card once.
3. Deploy: `vercel --prod --token "$VERCEL_TOKEN_VISION" --scope team_hdxl43AlgWsesgr06UyM3Lnk` from the worktree.
4. Fast-forward main to task/alpha-program and push.

## Open items for Yousif
- Shopify: Settings > Users > Roles > Agent > Apps and channels > tick "Manage and install apps and channels" > Save. App scopes are already released (alphaos-agent-pixart-3); knowledge/integrations/alphaos-pixart-shopify-access.md has the finish steps. Until then the staff-session sync keeps orders flowing (read only).
- Emails: the 12 PixArt templates are saved in prod; sending switches stay OFF (docs/PIXART_EMAIL_TEMPLATES.md explains both). A phishing mail sits in Needs you; ignore that sender.
- Real VA and designer accounts are created at rollout, after the admin lane confirms a VA can be added from the UI.
