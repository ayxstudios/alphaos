# QA 2026-09-25: admin lane (full test, debug, uncamel)

Owner's words: "test every function of alphaOs. go in like every user level and
test everything. do not stop until you have tested and then debugged and
uncamelled every section and that you are 1000% happy with it and that this
work is the best work youve ever created."

Lane: admin. Branch `lane/2026-09-25-admin` in
`~/Documents/projects/alphaos-wt-admin`, based on `task/alpha-program` 9e7ec97.
Nothing pushed, merged or deployed. Surfaces: staging
(https://alphaos-staging.vercel.app, mocks armed) signed in as the staging
admin, and a local dev server (port 3473) on the local database
`alphaos_admin25` (tour seed + 8 weeks of history) for verifying fixes.
Laptop 1440x900 and phone 390x844 (touch, 3x) for every screen.

Scores 1 to 10: the lowest of (a) know what to do next, (b) copy short,
human, consistent, (c) nothing cut off, cramped or misaligned, (d) instant,
no flash or jump, (e) calm and finished. L = laptop, P = phone.

## Round 0: finishing the previous run's half-done work

The previous run was cut off with four uncommitted files. Finished and
committed before anything else:

| Commit | Fix |
| --- | --- |
| f3662c9 | Proof email text: "Attached to this email are the high-resolution images", "portraits exceed" |
| d81d5b7 | Settings: every save (Etsy keys, cutoff, Shopify keys, Gmail keys, print keys, print mappings, templates) returns `{ ok, message }` and the form shows a toast in plain words instead of a thrown error or silence; Shopify save says whether instant orders (webhook) were set up; turning customer email ON asks first in a drawer and is disabled until Gmail is connected; Gmail card copy in plain words |
| ebb08f9 | Email templates: each shows what it is for, a Preview with sample details (no `{{...}}` left), and a placeholder the email cannot fill (a typo, or `{{tracking_url}}` in the photo request) is flagged live and refused on save |

## Known findings from the coordinator's sweep

| Finding | Root cause | Commit |
| --- | --- | --- |
| /designers/[id] "Minified React error #418" | Reproduced on staging by moving the browser clock 5 minutes ahead (a VA laptop whose clock is off, or the minute ticking over between server render and hydration): `Countdown` (components/board, designer lane) reads `Date.now()` on the server and again on the client, so the "9h 25m left" text differs. The designer lane already committed the fix (34afce1, `suppressHydrationWarning` on the countdown text); not duplicated here. Verified below. | designer lane 34afce1 |
| Home chart text under 11px | Bar chart axis text was 10px, below the 12px floor of the type scale | 1c4411b (12px, gutter sized to the widest tick). The designer lane moved the same lines to 11px (30cf351): at merge take this lane's version |
| /styles 16x16 and /settings 13x13 checkboxes on a phone | Bare checkboxes, one with no size at all | 7cd18bd (20px box, the label row is the 44px tap area) |

## Round 1 (staging walk + local actions)

Staging (9e7ec97, signed in as the staging admin) for the look and the mocked
vendor calls; the local dev server for every write (keys, templates, switches,
Money, Team) so staging's shared rows stay as other lanes expect. The iMac's
load average ran 100 to 900 during this round (four lanes), so load times
below are not the app's.

What was walked and done so far: Home (every tile and chart), Settings every
section on both sizes with every panel opened, and these actions: business
rename and revert, a logo link that is not a picture, an empty name; a
template with a typo placeholder (flagged, Save disabled), Preview (no
`{{...}}` left), Save, Reset; Send test (staging mock mailbox: 12 sent);
Check replies (staging: 1 attached); customer email off, then on through the
new confirm drawer; order updates off and on; morning briefing save; reminder
test; Gelato keys save and remove; Etsy keys save, cutoff save, Sync now,
Re-import 60 days through its drawer; Shopify Test (staging mock: connected),
keys save, Register webhooks, Sync now. No browser alert or confirm appeared
anywhere (every dialog is logged by the harness; none fired).

### Defects found and fixed in round 1

| Commit | Defect (surface) | Fix |
| --- | --- | --- |
| 1c4806d | Phone account menu 36px wide (every page, P) | 44px |
| 6303429 | Count charts labelled the middle line "3" when it was 2.5 (Home, both) | whole-number gridlines |
| 0b7502c | Home "On time: Nothing shipped in 30 days" beside a chart and a Print card showing shipments (both); unmatched-messages link 36px (P) | honest hint; 44px |
| 119e669 | Checkboxes 13 to 20px (Settings, Portrait Styles, P); chip remove buttons 13px (Settings, P) | new `components/ui/checkbox.tsx`: the real input is the 44px tap area on a phone, 16px box on a laptop; chip removes 44px |
| 7ee20b1 | Phone header: the workspace switcher's arrow spilled out of its box over the search icon (P) | tighter phone gaps, name truncates inside the box |
| bc0f674 | Gmail return address broke mid-word ("localhost:347 / 3"); template names cut off on a phone; "ETSY"/"SHOPIFY" tags in capitals | wrap at sensible points, names wrap, sentence case |
| e74efa6 | Reminder test said "40 fires would create 114 notifications", alert types as "order / overdue", roles lower case | plain alert names, "would go out", Admin/VA/Designer |
| 3d7c9e5 | Business details: a logo link that is not a picture showed a broken image; a failed save was an unhandled error | a clear line; error line |
| 93a6fee | Shopify Test broke with "body.errors.map is not a function" when Shopify answers a bad key or domain with a plain string | every Shopify error shape handled (lib/integrations/shopify/client.ts) |
| 8d6404d | Sync now, Re-import, Register webhooks, Check replies and the per-template Send test errors showed the raw thrown text ("Google token exchange failed: invalid_client"); in a production build Next replaces a thrown server-action message with "An error occurred in the Server Components render", so the admin saw that instead | these actions return `{ ok, message }` with one plain line ("Gmail needs reconnecting. Press Reconnect Gmail and sign in again."); the raw error goes to the log |
| 9725c04 | Staging: after Register webhooks the Shopify card still said "Not pointing at AlphaOS": the mock listed `/api/webhooks/shopify/orders-create`, a path the app does not use | the mock remembers the registered address (lib/mock/transport.ts) |

Not a defect, noted: Settings "product mappings" (components/settings/print-product-mappings-panel.tsx) is not on any page; it was taken out on purpose in 81b9d47 (manual print signal). Left out; its save now reports like the others in case it comes back. "Logo upload" is a logo link field by design (no file upload in Settings).

### Round 1, continued: Money, System Health, Designers, Team, Portrait Styles

Money (local, 8 weeks of history): September view, View orders drilldown, Mark
paid drawer opened and cancelled, CSV fetched (200, `text/csv`, 30 rows = the
30 September earnings; per designer the page's To pay / Paid match the
database to the cent: Dex $27.50 / $18.00, Dina $30.00 / $65.50, Dana $44.00 /
$10.00, one $6.00 voided). Staging has no earnings this month (empty state
reads right).

Team (staging, throwaways "QA Lane VA/Admin/Designer <stamp>", emails
`@alphaos.test`): add VA, add admin, add designer (each shows the password
once), a second account with the same email is refused ("An account with that
email already exists"), sign-in link made and opened in a fresh browser (lands
on Home signed in as the VA), revoked (the same link then shows "This link no
longer works ... Sign in with your password, or ask your admin"), password
reset (the drawer says open sessions ended and the link stopped). Deactivate,
reactivate and cleanup: see the Team result below.

| Commit | Defect (surface) | Fix |
| --- | --- | --- |
| 2ddff65 | Money dates "03 Sept" beside "23 Sept"; a thrown Mark paid / Void / Resolve left the button spinning with no message | "3 Sept"; a toast |
| 9e40352 | System Health took 12 to 25 s on staging (Home 8 s): ~25 queries back to back in one transaction | the reads run together in short transactions (lib/health/daily-report.ts, also used by the morning email) |
| 057b5a6 | Text boxes and dropdowns 40px tall on a phone (every form) | 44px below `sm` (components/ui input, select) |
| c884606 | Designer contact: Timezone and Max active orders labels not tied to their boxes (tap on the label did nothing, screen readers read no name) | the app's Input |
| 4e1fc61 | /designers/[id] title sat 280px right of every other page's title (narrow centred page) | default width, the week in a phone-width column, "Designers" link above the name |
| 58fa570 | Team on a phone: three full-width buttons under every person (a wall of 18 buttons for 6 people) | one "Manage" menu per person on a phone (same three actions); laptop unchanged |
| 6277d02 | Portrait Styles: the default style's rate box sat 80px right of every other card's | badges after the rate |
| 3c7e6b9 | Products to confirm said "Went to cartoon by default", "Confirm 2 as cartoon" while every other screen says "Cartoon" | the app's style names |
