# AlphaOS full test + debug + uncamel program, 2026-09-25 (resume sheet)

Owner order: test every function at every user level, debug, uncamel every section; plus (same day)
"Sign your name:" on QC (done, 282ed83 + 94c519b on task/alpha-program), a simplicity pass by a naive
user with QC first (done), a tour rebuilt to POINT (circle + arrow, the person clicks, normal pace,
no ghost cursor; in progress), owner walkthrough logins (SENT 15:50 to both owners' WhatsApp; vault keys
ALPHAOS_VA_<NAME>_* and ALPHAOS_STAGING_DESIGNER_<NAME>_*; one line to each owner when the new build is live).

## Lanes (branch, worktree ~/Documents/projects/alphaos-wt-<lane>, doc docs/QA-2026-09-25-<lane>.md)
| lane | branch | state |
|---|---|---|
| va | lane/2026-09-25-va | rounds in progress, 37+ commits |
| designer | lane/2026-09-25-designer | rounds in progress, 25+ commits |
| admin | lane/2026-09-25-admin | round 2 done, 34+ commits |
| customer-security | lane/2026-09-25-customer-security | DONE, 20 fixes, 26 commits |
| simplicity | lane/2026-09-25-simplicity | DONE, 12 commits (QC checklist rewrite, plainer words) |
| tour | lane/2026-09-25-tour (includes simplicity) | rebuild in progress: ring + arrow + card, person clicks |

## Progress 2026-09-26 00:05 AEST (merge done, verify green)

All six lanes are MERGED into task/alpha-program (commit chain up to the tour merge eb01d97 + fixes). Resolved as:
bars.tsx = admin's dynamic gutter; globals.css = 16px fields + 44px min-height on phone OR coarse pointer;
designer-board = designer lane's accessibility object; checklist-panel + components/tour/* + lib/tour/* +
docs/TOUR.md = tour lane (guide.ts print answer re-worded to Mark as sent / Add tracking); orders page,
staff-home, fail-dialog = admin + va. The VA skip-memory fix (7a2741e) is ported into the new tour runtime
(welcome and resume both honour it). `npm run lint`, `npx tsc --noEmit`, `bash scripts/ci-local.sh` (23/23;
the script now blanks R2 so the upload checks run) and `npm run build` are all green on the merged branch.

LOCAL WALKS: this worktree's .env.local points at PRODUCTION Neon. Every local run sources the shell
overrides (local Postgres alphaos_tour25, ws proxy 127.0.0.1:5502, app :3476, logins tour-<role>@alphaos.test
/ tourpass123 from scripts/tour-check.mjs): DATABASE_URL, DIRECT_URL, NEON_LOCAL_PROXY, AUTH_URL,
NEXT_PUBLIC_APP_URL, NODE_OPTIONS (neon-local require+import), R2_* blank, MOCK_INTEGRATIONS=1.
The built app was started that way (`next start -p 3476`) and the tour walk (steps 3 to 5 of the tour doc)
began on it; its state is in docs/QA-2026-09-25-tour.md "Where I stopped".

Still to do, in order: finish the tour walk; calm pass (brief below, phone first, repair never remove);
staging deploy + role walk; production deploy; fast-forward main; one line to each owner.

### Calm pass brief (was in a lost scratchpad)
Home on a phone: each chart folds behind one line with a chevron. Orders list on a phone: a smaller card
per order (number, customer, stage chip, due). Order page on a phone: less-used sections (history, raw
options, integration details) collapse behind their heading; status, customer, photos, next step stay open.
"Needs you" (Home + Today): spam and notification emails never count or show. Designer card: raw option
text summarised to one human line, full text one tap away. Judge each page 1..10 on a phone, loop to 9+,
write before/after into docs/CALM-PASS-2026-09-25.md.

## Merge plan (worktree of record ~/Documents/projects/alphaos-wt-alpha, branch task/alpha-program)
1. Merge in order: va, customer-security, admin, designer, simplicity, tour. Known conflict spots from a
   trial merge: components/charts/bars.tsx + app/globals.css (designer vs admin chart label size),
   components/qc/checklist-panel.tsx (va tap targets vs simplicity wording), lib/tour/steps.ts + docs/TOUR.md
   (designer vs simplicity/tour). Take the tour lane's version of components/tour/* and lib/tour/player.ts.
2. Verify: npm run lint, npx tsc --noEmit, npm run build, bash scripts/ci-local.sh (23 suites, +9 security checks),
   npm run test:tour (rewritten by the tour lane).
3. Calm pass on the merged code (phone layout for Home, Orders list, Order page, Messages, designer card):
   brief was in the session scratchpad CALM-PASS-BRIEF.md; summary: fold charts behind one line on a phone,
   smaller Orders phone card, collapse less-used order page sections, spam out of "Needs you", summarise
   raw option text on designer cards. Repair never removes.
4. Redeploy staging (scripts/staging/deploy-preview.sh with VERCEL_TOKEN_VISION + STAGING_ENV_FILE), walk every
   role once more (page-check.mjs --state <storageState.json> now works for logged-in pages), then
   `vercel deploy --prod --yes --token "$VERCEL_TOKEN_VISION" --scope team_hdxl43AlgWsesgr06UyM3Lnk`,
   verify health + both owner links, fast-forward main, push, one line to each owner.

## Staging state
Cloned from production 2026-09-24 23:57, mocks armed, latest base 9e7ec97 deployed 00:03. Lanes changed
orders on it (see each lane's doc). Owner designer accounts ayser.designer@ / yousif.designer@alphaos.test
live there with two orders each; a re-clone wipes them (recreate with create-user + attach, see memory).

## Lessons
Subagents must be launched with model opus/sonnet, never inherited Fable (four lanes died twice on credits);
lanes commit after every fix and keep their doc current; the iMac load hit 950 with five lanes running dev
servers and browsers at once: one dev server + one browser per lane, no `next build` in lanes.

## Progress 2026-09-26 ~06:40 AEST (tour walk DONE, calm pass DONE and MERGED, verify in flight)

- Tour walk finished on task/alpha-program (commits c8a0534 .. 2bd3c29): final tour-check 1298 passed, 0 failed,
  all roles both viewports + Later; 79 of 81 screenshots scored 9, two 8s (designer phone card step: the arrow from
  the bottom sheet crosses the "Add the portrait" label; fix idea = sheet under the target once the gap passes ~120px).
  TOUR.md "Last measured" filled. Known: the Next router can hold a finished navigation until its next action (tour
  saves first as a workaround).
- Calm pass done on lane/2026-09-26-calm (17 commits, docs/CALM-PASS-2026-09-25.md, every phone page 9) and MERGED
  clean into task/alpha-program as a402602. New: lib/email/noise.ts (notification/marketing mail out of "Needs you",
  test:email-noise), components/ui/phone-fold.tsx, lib/orders/options-summary.ts. Left open by that lane: order page
  Edit button wraps on phone; Columns button shows on phone Orders; sla-sweep.ts + daily health still count noise mail.
- On a402602: lint green, tsc green. bench GREEN 24/24 (var/ci-local-merge.log); IN FLIGHT when usage ran out:
  build green; tour-check on the merged build (governor exemptions via mac-governor fg, else the browser dies under swap): 1114 passed 0 failed full run + VA phone rerun 270 passed 0 failed (the first full run lost VA phone to a 30s wait at load 22, not a defect).

NEXT, in order: (1) read both logs; if the bench or build failed, fix and commit. (2) `source <scratchpad>/local.env &&
nohup npx next start -p 3476 > var/next-start.log 2>&1 &` then `node scripts/tour-check.mjs --base http://localhost:3476`
(the calm pass changed the pages the tour rings: Orders phone card keeps the order:customer marker, order page folds
sections, Home charts fold). Expect 0 failed; look at var/tour-shots phone shots for the folded pages. (3) Staging:
`STAGING_ENV_FILE=~/Documents/ai-employee-agent/.local/alphaos-staging.env VERCEL_TOKEN=$VERCEL_TOKEN_VISION
scripts/staging/deploy-preview.sh`, walk admin/va/designer once (page-check.mjs --state). (4) Prod:
`vercel deploy --prod --yes --token "$VERCEL_TOKEN_VISION" --scope team_hdxl43AlgWsesgr06UyM3Lnk`, health + both owner
links, fast-forward main, push, one line to each owner (Ayser + Yousif own DMs: "the new build is live").
Local env recipe lives in the "LOCAL WALKS" paragraph above (local.env = those exports; calm.env = same with
alphaos_calm26 + :3477). Worktree alphaos-wt-calm can be removed after the deploy.

## DONE 2026-09-26 06:45 AEST: program shipped

Staging redeployed from a402602+ and walked (page-check --state, admin/va/designer, laptop + phone, every real
route clean; the only warning is the 20px select checkbox on phone Orders). Production deployed 06:38 (Ready, health
ok, all four owner login links answer 200), main fast-forwarded to 5d6d52a, one line sent to each owner.
Open follow-ups (not blockers): designer phone card arrow crosses the "Add the portrait" label (two 8s); order page
Edit button wraps on phone; Columns button shows on phone Orders; sla-sweep + daily health still count notification
mail; 20px select checkboxes on phone Orders. Local :3476 server stopped, worktree alphaos-wt-calm removed (branch kept).
