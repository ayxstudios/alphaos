# QA 2026-09-23: VA journey on staging

Walked on https://alphaos-staging.vercel.app as staging-va@alphaos.test,
headless, laptop 1280x800 and phone 390x844. Screenshots:
`var/e2e-shots/va/` (gitignored). Stopped early at the coordinator's request.

## Walked

1. Sign in, welcome card, 7-step tour (Start, Next, Try it on every step,
   Done), "?" > Show me around, Quick guide (/help), Later + reload, sign out
   + sign in again, resume after reload. All pass on laptop and phone.
2. Home and Today: every tile and link returns 200 and opens the right list.
3. Orders: every view tab, sort headers, page sizes, pagination, search by
   order number and by name, Filters, Columns, bulk bar. Order detail page,
   Complete details (awaiting_details -> awaiting_photos without a photo).

## Not walked yet

Step 3 end (photo -> ready_to_assign + auto-assign), step 4 (assign, designer
upload, QC fail/pass, proof email), 5 (Messages), 6 (Print), 7 (Customers,
phone tabs).

## Fixed (branch commits)

- P2 `0ff1dbb` Home "Do first" rows were not tappable on a phone (the action
  button is hidden below `sm` and the row was not a link).
- P2 `8a1bc94` Orders search by customer name found nothing for imported Etsy
  orders (98 of 126 have no customers row; the shown name comes from
  `raw_import.name`). Search now also matches receipt name and buyer email.
- P2 `e4a0437` React hydration error #418 on every /orders page: the client
  table formatted dates in the viewer's time zone, the server in UTC. Pinned to
  Australia/Melbourne.
- P2 `4340bef` Complete details / New order showed "No styles configured"
  while the business has 4 styles on /styles (forms read only the legacy
  `shops.styles`). Now falls back to the business styles, validation too.

## Remaining

- P1 (staging environment, not code) R2 bucket CORS allows
  alphaos-kappa.vercel.app and localhost:3000 but not
  alphaos-staging.vercel.app (preflight 403). Every browser upload on staging
  fails (VA reference photos, designer portraits, customer upload page). Add
  the staging origin to the bucket CORS rules (owner decision: it is the
  production bucket). Workaround used: Playwright route shim doing the PUT
  from node.
- P2 After a photo upload on Complete details, the "Save details" button was
  not found within 30 s (label changes to the photo variant; not yet
  confirmed if it is a harness selector or a real hang). Repro: open an
  awaiting_details order > Complete details > add a photo > Save.
- P2 Order detail shows "Defaulted to , not matched, please confirm" with an
  empty style name (order 4170595372 after completing details with no style).
  `components/orders/order-style-setter.tsx` line 84.

## P3 (logged, not fixed)

- Today: "Show 7 more of 7" reads oddly when all hidden rows fit in one page.
  `components/today/today-queue.tsx` line 229.
- Orders: Columns popover does not close on Escape.
- Orders: bulk bar wraps "Change status" onto a second line at 1280 px.
- Orders: the floating Alpha AI button covers the last row's action button
  (bottom right) on laptop.
- Orders Filters: the five selects have no name or aria-label; status option
  text "Awaiting Qc" (should be QC).
- Top bar: notifications button is 36 px on a phone, the help button 44 px.
- Phone only: an `OPTIONS /` request returns 400 on each page load (not from
  app code; likely preview tooling).
