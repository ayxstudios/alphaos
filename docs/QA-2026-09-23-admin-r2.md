# QA 2026-09-23: admin journey, round 2

Staging https://alphaos-staging.vercel.app (mock-armed) as staging-admin,
headless Chromium, laptop 1280x800 and phone 390x844 (Melbourne time zone).
Fixes re-verified on a local dev server against `npm run db:local-tour`.
Screenshots: `var/e2e-shots/admin-r2/` (gitignored; `L*` = local re-checks).

## Walked

1. Team panel: added a VA (details shown once, Copy puts link + email +
   password on the clipboard), signed in as that VA in a second context (VA
   nav, no Money/Settings/Health, Settings says "Managed by an admin"); added a
   designer; reset the VA's password (old session sent to /login, old password
   refused, new one works); deactivated (session ends, sign-in refused) and
   reactivated. Last admin: the panel never offers Deactivate on yourself, and
   the last active admin is always the viewer, so the refusal is server-only
   (covered by `test:team`). Phone layout: no sideways scroll, 44px buttons.
2. Portrait Styles: created a style with a per-figure rate, edited the rate
   (a negative rate is refused), deleted it. Shop import rules (figures,
   non-portrait) do not reference styles; the per-shop style list does (below).
3. Settings with mock credentials: Etsy and Shopify cards open, Connected,
   cutoff and rules save, Shopify Test says connected; Customer Email card
   connected, Check replies and Send test (12 templates OK, mock); templates
   editor loads all 12, save and reset; Print Providers save; Notifications
   recipients save and the dry-run report; every section on the phone.
4. System Health: honest (5 stale jobs, 2 stale syncs: crons never run on a
   preview), no narrative with the relay off.
5. Money: took PC32170 end to end with the new VA and designer (assign, Start,
   upload, Submit for QC on the phone board, QC pass with typed signature,
   proof email via mock, customer approves on /proof, bulk status Complete):
   $4.00 pending for QA R2 Designer; drilldown, CSV (14 columns, right row),
   Mark paid ($4.00 moves to Paid). A blocked row from another lane showed in
   the "Needs a rate" panel with Void.
6. Orders: manual order QA-R2-0001 (Shopify, digital, 2 figures, cartoon)
   with a dropped image and an image URL, Enter submits; duplicate lookup on
   PC32170 links to the existing order; Re-resolve on the Shopify shop
   (36 orders); bulk status; workspace switcher lists only PixArt.
7. Round 1 P2s: fixed below.
8. Phone: Settings (all sections), Payouts (+ drilldown), Team panel,
   Portrait Styles, Health, New order.

## Defects

### Fixed (P2)

- 4f1fd5e Server-rendered times were UTC with no label (Health, Money,
  order page, customers, board, print queue, emails); an order placed 1:48 am
  AEST read as the previous day. Shop cards, QC version strip and the new
  order form used the browser zone and threw React #418 on hydration. Now all
  go through `lib/time.ts` (Melbourne, "AEST"/"AEDT" on any time of day).
- 6c4497d Template Reset to default kept the edited text until a reload;
  Save gave no confirmation.
- 516436c No switch for `stage_email_auto_send`; the sending switch was not
  explained when ON; the Gmail form spilled past the card on a phone.
- fd2dc2a No way to set the business name or logo (`logo_url` shows on the
  upload and proof pages): new Settings > Business card.
- fd8a995 Settings > Portrait Styles said "0 styles" per shop while the
  checklist said "N styles" (empty = every style). The shop list also took
  free text, so a typo or case mismatch tagged manual orders with a rateless
  style and blocked pay; now catalog names only.
- b29b1e9 A pending earning could not be voided (Void only on blocked rows),
  so an order completed by mistake would be paid by the next Mark paid.

### Open (P2, not in this lane)

- Shopify line "Print On: Digital File Only" imports as Physical (the Shopify
  variant requires shipping), so PC32170 said "Approved, start the print &
  ship job" and got the physical proof email. Import/classification lane.

### P3 (logged, not fixed)

1. Add designer drawer says "no styles"; the roster shows "Any style" and
   Portrait Styles shows "Designers · 0 / No designers assigned yet" even
   though an any-style designer takes every style.
2. Portrait Styles: a refused rate stays in the field (the toast says it did
   not save, the input still shows "-3").
3. Deleting a style gives no warning that orders tagged with it will block
   their designer's pay.
4. Settings: Save cutoff gives no confirmation.
5. Money: Mark paid shows with $0.00 pending and gives no toast after paying.
6. /orders/new does not focus Order number on load (keyboard-first form).
7. A manual order with photos says "Lands in ready-to-assign & auto-assigns"
   but QA-R2-0001 stayed Unassigned with no reason shown.
8. Shopify order page: the draft reply says "paste it into the Etsy
   conversation"; "1 figures" on the order page and orders table.
9. No manual archive for an order (only Cancel).
10. Team panel tabs wrap to two lines at 390px.
11. The first Designers-board "Welcome" card covers the card modal's upload
    area on the phone until dismissed.

## Staging data left behind

Users qa-r2-va@ and qa-r2-designer@alphaos.test (active), order PC32170
completed with a paid $4.00 earning, manual order QA-R2-0001 (ready to
assign), one upload in R2 under a new key.

## Decisions

Polish pass 2026-09-24 (fixes: d664edf, b6be5cf, 98bf135, 382c4db, 59ad4be, 94b4864, 1538805, dc11426, 2e189b3). Not changed here, and why:
- P3 1: the premise was off. Auto-assign is strict on style, so a designer with no styles only gets orders with no style; the roster's "Any style" was the wrong label and now reads "No styles yet" (d664edf), matching Add designer and Portrait Styles.
- P3 9 (manual archive): a new feature (archive semantics touch sync, retention and every list), not a polish fix.
- P3 11 (Designers-board Welcome card over the card modal on a phone): components/board belongs to the designer lane working in parallel.
- Open P2 (Shopify "Digital File Only" imported as Physical): fixed in the import lane (2ee1d59), not reworked here.
- Staging data left behind: test users and orders on staging, nothing to fix in code.
