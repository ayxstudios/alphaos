/**
 * Shared style fragments for UI primitives.
 * Every interactive element uses `focusRing` — a visible 2px --pigment ring.
 */
export const focusRing =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-pigment " +
  "focus-visible:ring-offset-2 focus-visible:ring-offset-canvas";
